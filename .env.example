# Port the Fastify server binds to. Fly.io and most hosts inject this
# automatically; in local dev it defaults to 4000.
PORT=4000

# Optional extra origin added to the CORS whitelist (useful for
# previewing from a custom domain without redeploying).
# CORS_EXTRA_ORIGIN=https://deuna.example.com
